{
  "private": true,
  "scripts": {
    "pub": "yarn build && lerna publish",
    "build": "lerna run build --parallel --",
    "flow": "blueflag-test flow",
    "lint": "blueflag-test lint",
    "test": "blueflag-test test packages/**/*-test.js",
    "coverage": "blueflag-test coverage",
    "test-all": "yarn lint && yarn flow && yarn coverage"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.23.0",
    "babel-preset-blueflag": "^0.6.0",
    "blueflag-test": "^0.12.0",
    "enzyme": "^2.6.0",
    "lerna": "^2.9.0",
    "proto-babel-plugin-extract-flow-types": "git+ssh://git@github.com/blueflag/proto-babel-plugin-extract-flow-types.git#v0.1.1",
    "react": "^15.4.2",
    "react-addons-test-utils": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-redux": "^5.0.6",
    "redux": "^3.6.0",
    "require.async": "^0.1.1"
  }
}
