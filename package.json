{
  "release": {
    "name": "AutomaticCrow"
  },
  "private": true,
  "scripts": {
    "prep": "yarn && yarn lerna bootstrap",
    "build": "lerna run build --parallel --",
    "test": "lerna run test --parallel --"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
