{
  "name": "enty",
  "version": "1.1.0",
  "description": "An opinionated set of redux tools to cut down on boilerplate",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "Allan Hortle",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/blueflag/enty.git"
  },
  "files": [
    "dist"
  ],
  "bugs": {
    "url": "https://github.com/blueflag/enty/issues"
  },
  "private": false,
  "scripts": {
    "build": "yarn tsdx build",
    "lint": "yarn tsdx lint src",
    "prepublishOnly": "yarn build",
    "test": "yarn jest",
    "test-all": "yarn lint && yarn  && yarn test",
    "watch": "yarn tsdx watch"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.6.0",
    "tsdx": "^0.14.1"
  },
  "dependencies": {
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "unmutable": "^0.46.0"
  }
}
