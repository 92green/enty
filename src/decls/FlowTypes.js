/* @flow */
/* eslint-disable no-unused-vars */

import {Map} from 'immutable';

type NormalizeState = {
    entities: Object|Map,
    result: Object|Map
};


type SelectOptions = {
    schemaKey?: string,
    stateKey?: string
};
